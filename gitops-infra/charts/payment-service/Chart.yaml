apiVersion: v2
name: payment-service
description: A Helm chart for deploying the payment-service microservice on EKS
type: application

# This is the version of the Helm chart itself
version: 0.1.0

# This is the version of your actual application container code
appVersion: "1.0.0"